<H1>Category index</H1>

<% @categories.each do |category| %>
    <h3> <%= category.name %></h3>
    <h3> <%= image_tag category.photo, width:300, height: 300 %>

<%= link_to "lien vers la cat", category_spots_path(category) %>
  <% end %>

<div>
